@model IEnumerable<MastersDetailsInMvc.Models.Catagory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatogoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatogoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CatagoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CatagoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CatagoryId })
        </td>
    </tr>
}

</table>
